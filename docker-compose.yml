services:
  standalone:
    build:
      context: .
      dockerfile: Dockerfile
    image: versera-production
    container_name: versera-production
    environment:
      NODE_ENV: production
      PORT: "3000"
    ports:
      - "3000:3000"
    restart: unless-stopped
